Se tiene un DataFrame de pandas que contiene valores faltantes (NaN) en columnas numéricas.

Escribe una función llamada preparar_datos(df, target_col) que:

1. Separe X e y usando target_col
2. Impute los valores faltantes usando la media (SimpleImputer de sklearn)
3. Escale los datos con StandardScaler
4. Devuelva X e y como arrays de numpy
